export const initAnalytics = () => {
    // Google tag is configured directly in index.html to ensure there is exactly one
    // GA4 tag loaded immediately after the head element and to avoid duplicate tags.
};
